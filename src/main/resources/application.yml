spring:
  datasource:
    url: 'jdbc:h2:~/member;'
    driver-class-name: 'org.h2.Driver'
    username: sa
  jpa:
    database-platform: 'org.hibernate.dialect.H2Dialect'
    hibernate:
      ddl-auto: create
    show-sql: true
  h2:
    console:
      enabled: true
